import React from 'react';
import RatingChart from '../../components/RatingChart';

const Player = () => {
    return (
        <div>
            <RatingChart />
        </div>
    );
};

export default Player;
